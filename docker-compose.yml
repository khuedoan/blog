version: '3'

services:
  web:
    image: node
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    entrypoint: "/bin/sh -c"
    command: "'npm install && npm start'"
    ports:
      - '3000:3000'
